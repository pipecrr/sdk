@page "/"
@attribute [Authorize]
@using Siesa.SDK.Shared.Application

@if(SDKApp.GetIndexComponent == null) 
{
    <PageTitle><ResourceViewer ResourceTag="Custom.Project.Name" /></PageTitle>

    <ResourceViewer ResourceTag="Custom.Project.Name" />
}else{
    <DynamicComponent Type="@SDKApp.GetIndexComponent()" />
}
namespace Siesa.SDK.Backend.Access
{
    //TODO: MOver al archivo de enums cuando se agregue el enum desde el gestor de tablas
    public enum EnumDBType {
        SQLServer,
        PostgreSQL,
        Unknown,
        InMemory
    }
}